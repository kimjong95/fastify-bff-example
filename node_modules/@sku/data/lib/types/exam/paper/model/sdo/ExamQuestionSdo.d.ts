import { QuestionType } from '../vo/QuestionType';
import { QuestionAnswer } from '../vo/QuestionAnswer';
import { QuestionItem } from '../vo/QuestionItem';
export interface ExamQuestionSdo {
    sequence: number;
    groupName: string;
    mandatory: boolean;
    point: number;
    questionType: QuestionType;
    question: string;
    imagePath: string;
    items: QuestionItem[];
    questionAnswer: QuestionAnswer;
}
