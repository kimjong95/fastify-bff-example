import { DomainEntity } from '../../../shared';
import { LectureType } from './vo/LectureType';
export interface MappedExamLecture extends DomainEntity {
    lectureType: LectureType;
    lectureId: string;
    paperId: string;
}
