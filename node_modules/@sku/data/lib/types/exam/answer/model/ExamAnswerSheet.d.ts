import { DenizenKey, DomainEntity } from '../../../shared';
import { ItemAnswer } from './vo/ItemAnswer';
import { AnswerState } from './vo/AnswerState';
export interface ExamAnswerSheet extends DomainEntity {
    denizenId: string;
    lectureId: string;
    paperId: string;
    answers: ItemAnswer[];
    obtainedScore: number;
    finished: boolean;
    answerState: AnswerState;
    grader: DenizenKey;
    graderComment: string;
    trials: number;
    scoreList: number[];
    time: number;
}
