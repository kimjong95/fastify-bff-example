import { LangSupport, DeviceType } from '../../../../shared';
import { BannerImage } from '../vo/BannerImage';
export interface BannerCdo {
    name: string;
    exposureType: DeviceType | null;
    images: BannerImage[];
    imageVersion: number;
    target: string;
    targetUrl: string;
    version: number;
    langSupports: LangSupport[];
    bgColor: string;
}
