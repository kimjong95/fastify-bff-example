import { DeviceType, DramaEntity, LangSupport, PolyglotString } from '../../../shared';
export interface LatestBanner extends DramaEntity {
    langSupports: LangSupport[];
    name: string;
    imageUrl: PolyglotString;
    imageAlt: PolyglotString;
    target: string;
    registrantName: PolyglotString;
    registeredTime: number;
    imageVersion: number;
    targetUrl: string;
    bgColor: string;
    exposureType: DeviceType;
    id: string;
}
