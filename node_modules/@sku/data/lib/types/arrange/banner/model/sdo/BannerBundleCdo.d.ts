import { ExposureType, GroupBasedAccessRule, LangSupport } from '../../../../shared';
import { OrderedBannerId } from '../vo/OrderedBannerId';
export interface BannerBundleCdo {
    name: string;
    startDate: number;
    endDate: number;
    bannerIds: OrderedBannerId[];
    groupBasedAccessRule: GroupBasedAccessRule;
    langSupports: LangSupport[];
    top: boolean;
    topBgColor: string;
    enabled: boolean;
    exposureType: ExposureType;
}
