import { DeviceType, DramaEntity, LangSupport, PolyglotString } from '../../../shared';
import { BannerImage } from './vo/BannerImage';
export interface Banner extends DramaEntity {
    langSupports: LangSupport[];
    name: string;
    images: BannerImage[];
    target: string;
    registrantName: PolyglotString;
    registeredTime: number;
    imageVersion: number;
    targetUrl: string;
    bgColor: string;
    exposureType: DeviceType;
}
