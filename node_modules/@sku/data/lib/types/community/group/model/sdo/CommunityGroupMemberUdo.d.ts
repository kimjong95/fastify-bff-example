import { DenizenKey } from '../../../../shared';
export interface CommunityGroupMemberUdo {
    denizenKey: DenizenKey;
    memberId: string;
    groupId: string;
    communityId: string;
    isAdmin: boolean;
}
