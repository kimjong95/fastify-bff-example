import { DenizenKey } from '../../../../shared';
import { AudienceKey } from 'shared/model/patron/AudienceKey';
export interface CommunityLikeRdo {
    denizenKey?: DenizenKey;
    postId: string;
    memberId: string;
    AudienceKey?: AudienceKey;
    loginId?: string;
    cineroomId?: string;
    pavilionId?: string;
}
