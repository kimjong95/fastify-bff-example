import { SkillRequirement } from './vo/SkillRequirement';
import { DomainEntity } from '../../../shared';
export interface Skill extends DomainEntity {
    name: string;
    requirements: SkillRequirement[];
    fileBoxId: string;
}
