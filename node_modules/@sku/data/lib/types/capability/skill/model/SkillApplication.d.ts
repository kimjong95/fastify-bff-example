import { DomainEntity } from '../../../shared';
import { SkillApplicationAnswer } from './vo/SkillApplicationAnswer';
export interface SkillApplication extends DomainEntity {
    skillId: string;
    denizenId: string;
    answer: SkillApplicationAnswer;
    registeredTime: number;
    id: string;
    round: number;
}
