import { SkillApplication } from '../SkillApplication';
import { UserIdentity } from '../../../../shared';
export interface ApplicationWithUserIdentity {
    skillApplication: SkillApplication;
    userIdentity: UserIdentity | null;
}
