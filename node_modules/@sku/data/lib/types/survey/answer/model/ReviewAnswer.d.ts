import { DomainEntity } from '../../../shared';
export interface ReviewAnswer extends DomainEntity {
    surveyCaseId: string;
    denizenId: string;
    completeTime: number;
    sentence: string;
    itemNumber: string;
    registeredTime: number;
}
