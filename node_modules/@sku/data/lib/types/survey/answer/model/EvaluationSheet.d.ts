import { DramaEntity } from '../../../shared';
import { SurveyAnswer } from './SurveyAnswer';
export interface EvaluationSheet extends DramaEntity {
    questionCount: number;
    answers: SurveyAnswer[];
    surveyCaseId: string;
    answerSheetId: string;
}
