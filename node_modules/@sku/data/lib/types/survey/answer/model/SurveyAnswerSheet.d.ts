import { DomainEntity } from '../../../shared';
import { SurveyAnswerState } from './vo/SurveyAnswerState';
import { SurveyAnswer } from './vo/SurveyAnswer';
export interface SurveyAnswerSheet extends DomainEntity {
    denizenId: string;
    surveyFormId: string;
    contentId: string;
    state: SurveyAnswerState;
    answers: SurveyAnswer[];
    registeredTime: number;
    modifiedTime: number;
}
