import { AnswerItemType } from 'index';
export interface SummaryItems {
    answerItemType: AnswerItemType;
    numberCountMap: Map<string, number>;
    matrixItems?: {
        rowNumber: string;
        numberCountMap: Map<string, number>;
    }[];
    sentencesMap?: Map<string, number>;
    sentences?: string[];
    criteriaItemCountMap?: Map<string, number>;
}
