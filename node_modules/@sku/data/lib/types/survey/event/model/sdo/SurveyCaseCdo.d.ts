import { DatePeriod, DenizenKey, LangStrings } from '../../../../shared';
import { SurveyEvent } from '../SurveyEvent';
import { SurveyOperator } from '../SurveyOperator';
export interface SurveyCaseCdo {
    denizenKey: DenizenKey;
    managementNumber: string;
    titles: LangStrings;
    descriptions: LangStrings;
    surveyFormId: string;
    surveyEvent: SurveyEvent;
    period: DatePeriod;
    operator: SurveyOperator;
    anonymous: boolean;
    targetRespondentCount: number;
    supportedLanguages: string[];
    commentFeedbackId: string;
}
