import { DramaEntity, LangSupport, PolyglotString } from '../../../shared';
import { DesignState } from './vo/DesignState';
import { Criterion } from './vo/Criterion';
import { SurveyQuestion } from './SurveyQuestion';
export interface SurveyForm extends DramaEntity {
    title: PolyglotString;
    state: DesignState;
    langSupports: LangSupport[];
    criteria: Criterion[];
    pinned: boolean;
    useCommon: boolean;
    reviewIncluded: boolean;
    hideResult: boolean;
    questions: SurveyQuestion[];
    registrant: string;
    registeredTime: number;
    modifier: string;
    modifiedTime: number;
}
