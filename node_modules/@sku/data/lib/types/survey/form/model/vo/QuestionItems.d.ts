import { LangStrings } from 'index';
import { QuestionItemType } from './QuestionItemType';
interface Item {
    number: string;
    values: LangStrings;
}
interface ImageUrl {
    imageUrl: string;
    number: string;
}
export interface QuestionItems {
    answerType: QuestionItemType;
    items?: Item[];
    imageUrls?: ImageUrl[];
    maxLength?: number;
    criterionNumber?: string;
    columnItems?: Item[];
    rowItems?: Item[];
    multipleChoice?: boolean;
}
export {};
