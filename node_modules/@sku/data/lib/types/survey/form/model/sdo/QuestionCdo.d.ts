import { DenizenKey, LangStrings } from '../../../../shared';
import { Sequence } from '../vo/Sequence';
import { QuestionItemType } from '../vo/QuestionItemType';
export interface QuestionCdo {
    denizenKey: DenizenKey;
    sequence: Sequence;
    sentences: LangStrings;
    optional: boolean;
    visible: boolean;
    questionItemType: QuestionItemType;
    sentencesImageUrl: string;
    surveyFormId: string;
}
