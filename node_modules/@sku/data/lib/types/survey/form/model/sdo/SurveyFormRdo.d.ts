import { DramaEntity, PolyglotString } from '../../../../shared';
import { DesignState } from '../vo/DesignState';
export interface SurveyFormRdo extends DramaEntity {
    id: string;
    title: PolyglotString;
    state: DesignState;
    registrant: string;
    registeredTime: number;
}
