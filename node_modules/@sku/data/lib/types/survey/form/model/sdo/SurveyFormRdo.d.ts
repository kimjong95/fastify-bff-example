import { DenizenKey, LangString } from '../../../../shared';
import { FormDesigner } from '../FormDesigner';
import { DesignState } from '../vo/DesignState';
export interface SurveyFormRdo {
    name: string;
    creatorName: string;
    denizenKey: DenizenKey;
    managementNumber: string;
    title: LangString;
    formDesigner: FormDesigner;
    designState: DesignState;
    startDate: number;
    endDate: number;
    time: number;
    useCommon: boolean;
    finalCopy: boolean;
}
