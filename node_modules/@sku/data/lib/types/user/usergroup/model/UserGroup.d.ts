import { DomainEntity, PatronKey, PolyglotString } from '../../../shared';
export interface UserGroup extends DomainEntity {
    patronKey: PatronKey;
    name: PolyglotString;
    sequence: number;
    registrantName: PolyglotString;
    registeredTime: number;
    categoryId: string;
    enabled: boolean;
}
