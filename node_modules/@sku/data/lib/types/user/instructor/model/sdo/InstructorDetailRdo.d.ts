import { Instructor } from '../Instructor';
import { Invitation, User } from '../../../user/model';
export interface InstructorDetailRdo {
    instructor: Instructor;
    user: User;
    invitation: Invitation;
}
