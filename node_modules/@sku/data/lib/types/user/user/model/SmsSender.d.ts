import { DomainEntity } from '../../../shared';
export interface SmsSender extends DomainEntity {
    qualified: boolean;
    registrant: string;
    registeredTime: number;
    modifier: string;
    modifiedTime: number;
}
