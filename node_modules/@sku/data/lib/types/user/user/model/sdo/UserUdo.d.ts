import { UserGroupSequences } from '../../../../shared';
export interface UserUdo {
    email: string;
    userGroupSequences: UserGroupSequences;
    gender: number;
    birthYear: string;
    membershipManagedManually: boolean;
}
