import { DenizenKey } from '../patron';
import { StudentApprovalHistory } from './StudentApprovalHistory';
export interface StudentApproval {
    approver: DenizenKey;
    remark?: string;
    registeredTime: number;
    studentApprovalHistories?: StudentApprovalHistory[];
}
