import { DomainEntity } from '../../../shared';
export interface NoteContent extends DomainEntity {
    noteId: string;
    content: string;
    playSecond: number;
    registeredTime: number;
    modifiedTime: number;
}
