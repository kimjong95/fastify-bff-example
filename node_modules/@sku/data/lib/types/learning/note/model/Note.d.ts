import { CubeType, DomainEntity, PolyglotString } from '../../../shared';
import { NoteContent } from './NoteContent';
export interface Note extends DomainEntity {
    denizenId: string;
    collegeId: string;
    cubeId: string;
    cubeName: PolyglotString;
    cubeType: CubeType;
    cardId: string;
    cardName: PolyglotString;
    folderId: string;
    registeredTime: number;
    modifiedTime: number;
    noteContents: NoteContent[];
}
