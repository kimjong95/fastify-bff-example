import { DomainEntity } from '../../../shared';
export interface Playlist extends DomainEntity {
    denizenId: string;
    title: string;
    description: string;
    expose: boolean;
    cardIds: string[];
    thumbImagePath: string;
    commentFeedbackId: string;
    likeFeedbackId: string;
    registeredTime: number;
    modifiedTime: number;
}
