import { DomainEntity } from '../../../shared';
export interface ReplayLearningTime extends DomainEntity {
    denizenId: string;
    year: number;
    cubeId: string;
    collegeId: string;
    replayLearningSeconds: number;
    registeredTime: number;
    latestTime: number;
}
