import { CommunityOrderBy } from '../vo/CommunityOrderBy';
export interface PostStatisticsRdo {
    communityid: string;
    startDate: string;
    endDate: string;
    offset: number;
    limit: number;
    orderBy: CommunityOrderBy;
}
