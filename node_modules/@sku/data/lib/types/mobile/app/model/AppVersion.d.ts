import { DramaEntity } from '../../../shared';
import { MobileOS } from './vo/MobileOS';
export interface AppVersion extends DramaEntity {
    mobileOS: MobileOS;
    version: string;
    filePath: string;
    bundleIdentifier: string;
    registeredTime: number;
    registrantName: string;
    modifiedTime: number;
    modifier: string;
    modifierName: string;
}
