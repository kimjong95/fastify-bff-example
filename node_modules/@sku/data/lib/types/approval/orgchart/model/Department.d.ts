import { DomainEntity, PolyglotString } from '../../../shared';
export interface Department extends DomainEntity {
    code: string;
    name: PolyglotString;
    parentCode: string;
    level: number;
    sortOrder: string;
    chartDisplayed: boolean;
    managerId: string;
    chartId: string;
}
