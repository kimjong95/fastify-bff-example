import { Icon } from '../model';
/**
 * 아이콘 생성
 * @param groupId
 * @param chunk
 */
export declare function registerIcon(groupId: string, chunk: any): Promise<string>;
/**
 * groupId로 아이콘 전체 목록 조회
 * @param groupId
 */
export declare function findAllIconsByGroupId(groupId: string): Promise<Icon[]>;
