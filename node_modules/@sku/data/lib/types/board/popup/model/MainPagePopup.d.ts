import { DatePeriod, DramaEntity, LangSupport, PatronKey, PolyglotString } from '../../../shared';
export interface MainPagePopup extends DramaEntity {
    patronKey: PatronKey;
    langSupports: LangSupport[];
    title: PolyglotString;
    contents: PolyglotString;
    open: boolean;
    periodJson: string;
    registeredTime: number;
    modifier: string;
    modifiedTime: number;
    period: DatePeriod;
}
