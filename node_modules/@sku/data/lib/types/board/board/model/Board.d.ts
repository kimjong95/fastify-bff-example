import { DramaEntity, IdName, LangSupport } from '../../../shared';
import { BoardType } from './vo/BoardType';
import { BoardConfig } from './BoardConfig';
export interface Board extends DramaEntity {
    langSupports: LangSupport[];
    boardId: string;
    builder: IdName;
    builtTime: number;
    boardType: BoardType;
    title: string;
    config: BoardConfig;
}
