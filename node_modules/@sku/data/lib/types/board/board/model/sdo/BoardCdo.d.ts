import { IdName, LangSupport } from '../../../../shared';
import { BoardType } from '../vo/BoardType';
import { BoardConfig } from '../BoardConfig';
export interface BoardCdo {
    langSupports: LangSupport[];
    builder: IdName;
    boardType: BoardType;
    config: BoardConfig;
}
