import { CloseOption } from './vo/CloseOption';
import { Writer } from './vo/Writer';
import { PostContents } from './PostContents';
import { PostConfig } from './PostConfig';
import { DatePeriod, ExposureType, IdPolyglotName, LangSupport } from '../../../shared';
import { PolyglotString } from '@sku/core';
import { OpenState } from './vo/OpenState';
export interface PostModel {
    boardId: string;
    category: IdPolyglotName;
    CloseOption: CloseOption;
    commentFeedbackId: string;
    config: PostConfig;
    contents: PostContents;
    deleted: boolean;
    ExposureType: ExposureType;
    id: string;
    langSupports: LangSupport[];
    openState: OpenState;
    patronKey: {
        keyString: string;
    };
    period: DatePeriod;
    pinned: boolean;
    postId: string;
    registeredTime: number;
    title: PolyglotString;
    writer: Writer;
    readCount: number;
}
