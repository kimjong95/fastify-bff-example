import { Writer } from '../vo/Writer';
import { PostContents } from '../PostContents';
export interface AnswerCdo {
    postId: string;
    writer: Writer;
    updater: Writer;
    title: string;
    contents: PostContents;
}
