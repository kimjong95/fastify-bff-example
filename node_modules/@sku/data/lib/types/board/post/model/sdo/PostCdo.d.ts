import { DatePeriod, IdName, IdPolyglotName, LangSupport, PolyglotString } from '../../../../shared';
import { Writer } from '../vo/Writer';
import { PostContents } from '../PostContents';
import { OpenState } from '../vo/OpenState';
import { CloseOption } from '../vo/CloseOption';
import { AlarmInfo } from '../AlarmInfo';
import { PostConfig } from '../PostConfig';
export interface PostCdo {
    langSupports: LangSupport[];
    boardId: string;
    title: PolyglotString;
    writer: Writer;
    contents: PostContents;
    config: PostConfig;
    category: IdPolyglotName;
    pinned: boolean;
    deleted: boolean;
    answered: boolean;
    answer: IdName;
    openState: OpenState;
    period: DatePeriod;
    alarmInfo: AlarmInfo;
    closeOption: CloseOption;
}
