import { LangSupport, PolyglotString } from '../../../../shared';
export interface PostRom {
    langSupports: LangSupport[];
    postId: string;
    title: PolyglotString;
    registeredTime: number;
    readCount: number;
    pinned: boolean;
}
