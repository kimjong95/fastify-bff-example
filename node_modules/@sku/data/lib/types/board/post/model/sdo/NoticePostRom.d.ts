import { ExposureType, LangSupport, PolyglotString } from '../../../../shared';
export interface NoticePostRom {
    langSupports: LangSupport[];
    postId: string;
    title: PolyglotString;
    registeredTime: number;
    readCount: number;
    pinned: boolean;
    commentCount: number;
    feedbackId: string;
    exposureType: ExposureType;
}
