import { DramaEntity } from '../../../shared';
import { Writer } from './vo/Writer';
import { PostContents } from './PostContents';
export interface Answer extends DramaEntity {
    postId: string;
    writer: Writer;
    updater: Writer;
    writtenTime: number;
    modifiedTime: number;
    title: string;
    contents: PostContents;
    answerId: string;
    commentFeedbackId: string;
}
