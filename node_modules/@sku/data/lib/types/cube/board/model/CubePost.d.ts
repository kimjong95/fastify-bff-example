import { DomainEntity, IdName, PatronKey } from '../../../shared';
import { PostBody } from './PostBody';
export interface CubePost extends DomainEntity {
    patronKey: PatronKey;
    title: string;
    writer: string;
    readCount: number;
    likeFeedbackId: string;
    registeredTime: number;
    commentFeedbackId: string;
    replies: IdName[];
    deleted: boolean;
    pinned: number;
    boardId: string;
    postBody: PostBody;
}
