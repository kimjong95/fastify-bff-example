import { DomainEntity, PatronKey } from '../../../shared';
export interface TaskCubeStudent extends DomainEntity {
    patronKey: PatronKey;
    cubeId: string;
    postCount: number;
    commentCount: number;
    subCommentCount: number;
}
