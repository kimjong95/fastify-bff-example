import { DomainEntity, PatronKey } from '../../../shared';
export interface DiscussionCubeStudent extends DomainEntity {
    patronKey: PatronKey;
    cubeId: string;
    commentCount: number;
    subCommentCount: number;
}
