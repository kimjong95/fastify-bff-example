import { PolyglotString, LangSupport } from '../../../../shared';
import { AreaType } from '../vo/AreaType';
export interface ContentsProviderCdo {
    areaType: AreaType;
    name: PolyglotString;
    langSupports: LangSupport[];
    enabled: boolean;
    isLink: boolean;
    phoneNumber: string;
    email: string;
    url: string;
    thumbnailPath: string;
    depotId: string;
    remark: string;
    pisAgree: boolean;
}
