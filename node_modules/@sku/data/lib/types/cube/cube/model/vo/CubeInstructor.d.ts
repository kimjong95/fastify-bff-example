import { RepresentativeInstructor } from '../../../../shared';
export interface CubeInstructor extends RepresentativeInstructor {
    lectureTime: number;
    instructorLearningTime: number;
    round: number;
}
