import { ExtraTaskStatus, LearningState, OrderDirection, ProposalState } from 'shared';
import { CubeOrderProperty } from './CubeOrderProperty';
export interface CubeWithStudentCountRdo {
    proposalState: ProposalState;
    learningState: LearningState;
    testStatus: ExtraTaskStatus;
    order: CubeOrderProperty;
    direction: OrderDirection;
    offset: number;
    limit: number;
}
