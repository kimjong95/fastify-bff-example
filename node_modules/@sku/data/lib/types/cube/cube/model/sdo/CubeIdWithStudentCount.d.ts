import { DramaEntity } from '../../../../shared';
import { StudentCount } from '../../../../lecture';
export interface CubeIdWithStudentCount extends DramaEntity {
    cubeId: string;
    round: number;
    commentFeedbackId: string;
    studentCount: StudentCount;
}
