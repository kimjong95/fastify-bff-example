import { CubeType, DramaEntity, PolyglotString } from '../../../../shared';
import { StudentCount } from '../../../../lecture';
export interface CubeWithStudentCount extends DramaEntity {
    cubeId: string;
    round: number;
    collegeId: string;
    commentFeedbackId: string;
    cubeType: CubeType;
    name: PolyglotString;
    studentCount: StudentCount;
}
