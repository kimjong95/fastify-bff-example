import { SortOrder } from '../../../../shared';
import { FileSortKey } from '../../../depot/model';
export interface DiskDrivePolicy {
    sortOrder: SortOrder;
    fileSortKey: FileSortKey;
    maxFileCount: number;
}
