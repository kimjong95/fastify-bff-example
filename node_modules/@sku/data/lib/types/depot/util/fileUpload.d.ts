export interface ResultReadFile {
    blob: Blob;
    name: string;
    size: number;
}
export declare function uploadFileBoxFiles(files: (File | ResultReadFile)[], depotId?: string): Promise<string | undefined>;
