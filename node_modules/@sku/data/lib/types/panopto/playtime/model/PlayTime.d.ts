import { DomainEntity } from '../../../shared';
export interface PlayTime extends DomainEntity {
    denizenId: string;
    cubeId: string;
    playedSeconds: number;
    duration: number;
    time: number;
}
