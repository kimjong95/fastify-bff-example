import { QuizQuestion } from '../QuizQuestion';
import { AlertMessage } from '../AlertMessage';
export interface QuizCdo {
    id: string;
    name: string;
    showTime: number;
    endTime: number;
    quizQuestions: QuizQuestion[];
    resultAlertMessage: AlertMessage;
}
