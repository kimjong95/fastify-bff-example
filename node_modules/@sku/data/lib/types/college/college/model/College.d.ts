import { DomainEntity, PatronKey, PolyglotString } from '../../../shared';
import { CollegeCreator } from '../../share/model';
export interface College extends DomainEntity {
    patronKey: PatronKey;
    name: PolyglotString;
    description: PolyglotString;
    panoptoFolderId: string;
    creator: CollegeCreator;
    displayOrder: number;
    registeredTime: number;
    enabled: boolean;
}
