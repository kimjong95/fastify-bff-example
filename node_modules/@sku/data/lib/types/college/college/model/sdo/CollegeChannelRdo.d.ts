import { PatronKey, PolyglotString } from '../../../../shared';
import { CollegeCreator } from '../../../share/model';
import { ChannelSdo } from './ChannelSdo';
export interface CollegeChannelRdo {
    id: string;
    patronKey: PatronKey;
    name: PolyglotString;
    description: PolyglotString;
    panoptoFolderId: string;
    creator: CollegeCreator;
    displayOrder: number;
    registeredTime: number;
    enabled: boolean;
    channels: ChannelSdo[];
}
