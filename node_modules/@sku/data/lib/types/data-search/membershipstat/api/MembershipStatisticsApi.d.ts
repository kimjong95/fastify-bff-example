import { AnnualMembershipStatistics, MonthlyMembershipStatistics, PassedStudent } from '../model';
/**
 * 연간 멤버쉽 통계
 * @param year
 */
export declare function findAnnualMembershipStatistics(year: number): Promise<AnnualMembershipStatistics>;
/**
 * 연간 멤버쉽 통계(mySUNI 매니저)
 * @param userWorkspaceId
 * @param year
 */
export declare function findAnnualMembershipStatisticsForMySuniManager(userWorkspaceId: string, year: number): Promise<AnnualMembershipStatistics>;
/**
 * 월간 멤버쉽 통계
 * @param year
 */
export declare function findMonthlyMembershipStatistics(year: number): Promise<MonthlyMembershipStatistics[]>;
/**
 * 월간 멤버쉽 통계(mySUNI 매니저)
 * @param userWorkspaceId
 * @param year
 */
export declare function findMonthlyMembershipStatisticsForMySuniManager(userWorkspaceId: string, year: number): Promise<MonthlyMembershipStatistics[]>;
/**
 * 성공 학생 목록 조회
 * @param year
 * @param month
 */
export declare function findPassedStudents(year: number, month: number): Promise<PassedStudent[]>;
/**
 * 성공 학생 목록 조회(mySUNI 매니저)
 * @param userWorkspaceId
 * @param year
 * @param month
 */
export declare function findPassedStudentsForMySuniManager(userWorkspaceId: string, year: number, month: number): Promise<PassedStudent[]>;
