import { DenizenKey, IdName } from '../../../../shared';
import { RemovePolicy } from '../vo/RemovePolicy';
export interface ReceiverCdo {
    denizenKey: DenizenKey;
    name: string;
    pavilion: IdName;
    removePolicy: RemovePolicy;
    time: number;
}
