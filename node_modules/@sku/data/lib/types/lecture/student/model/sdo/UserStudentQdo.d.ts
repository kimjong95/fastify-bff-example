import { LearningState, ProposalState } from '../../../../shared';
import { CardState } from '../../../card/model';
import { StudentType } from '../../../../shared';
export interface UserStudentQdo {
    userDenizenId: string;
    startDate: number;
    endDate: number;
    collegeId: string;
    channelId: string;
    hasStamp: boolean;
    cardState: CardState;
    learningState: LearningState[];
    proposalState: ProposalState;
    searchable: boolean;
    studentType: StudentType;
    learningStudentOnly: boolean;
    offset: number;
    limit: number;
}
