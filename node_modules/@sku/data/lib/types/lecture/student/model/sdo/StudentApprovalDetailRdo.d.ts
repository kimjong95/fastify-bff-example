import { SimpleUserIdentity } from '../../../../shared';
import { Card, EnrollmentCard } from '../../../card/model';
import { Student } from '../Student';
import { StudentApproval } from '../../../../shared';
export interface StudentApprovalDetailRdo {
    userIdentity: SimpleUserIdentity;
    card: Card;
    student: Student;
    enrollmentCard: EnrollmentCard;
    studentApproval: StudentApproval;
}
