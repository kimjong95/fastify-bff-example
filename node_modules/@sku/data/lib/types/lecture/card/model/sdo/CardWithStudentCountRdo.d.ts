import { ProposalState, LearningState } from '../../../../shared';
import { CardOrderProperty } from '../vo/CardOrderProperty';
import { OrderDirection } from '../../../../shared';
export interface CardWithStudentCountRdo {
    proposalState: ProposalState;
    learningState: LearningState;
    order: CardOrderProperty;
    direction: OrderDirection;
    offset: number;
    limit: number;
}
