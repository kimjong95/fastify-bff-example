import { DomainEntity } from '../../../shared';
export interface CardCollegeOrderInfo extends DomainEntity {
    collegeId: string;
    withNewCards: boolean;
    registrant: string;
    registeredTime: number;
    modifier: string;
    modifiedTime: number;
}
