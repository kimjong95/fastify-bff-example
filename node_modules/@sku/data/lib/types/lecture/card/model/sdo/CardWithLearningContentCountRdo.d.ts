import { CardRelatedCount } from '../CardRelatedCount';
import { Card } from '../Card';
export interface CardWithLearningContentCountRdo {
    card: Card;
    cardRelatedCount: CardRelatedCount;
    cubeCount: number;
    discussionCount: number;
    chapterCount: number;
    blendedChapterCount: number;
}
