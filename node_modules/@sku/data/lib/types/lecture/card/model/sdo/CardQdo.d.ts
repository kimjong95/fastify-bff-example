import { LearningState } from '../../../../shared';
import { DifficultyLevel } from '../../../../shared';
import { CardType } from '../vo/CardType';
import { CardOrderBy } from '../vo/CardOrderBy';
export interface CardQdo {
    collegeIds?: string[];
    channelIds?: string[];
    offset: number;
    limit: number;
    orderBy?: CardOrderBy;
    startDate?: number;
    endDate?: number;
    name?: string;
    registrantName?: string;
    difficultyLevels?: DifficultyLevel[];
    learningTimeRanges?: string[];
    hasStamp?: boolean;
    hasBadge?: boolean;
    required?: boolean;
    searchable?: boolean;
    instructorId?: string;
    startLearningDate?: string;
    endLearningDate?: string;
    type?: CardType[];
    studentLearning?: LearningState[];
}
