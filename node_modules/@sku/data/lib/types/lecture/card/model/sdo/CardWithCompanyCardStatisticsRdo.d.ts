import { PolyglotString, LangSupport } from '../../../../shared';
import { CardCategory } from '../vo/CardCategory';
export interface CardWithCompanyCardStatisticsRdo {
    id: string;
    cardId: string;
    cardName: PolyglotString;
    date: string;
    companyCode: string;
    count: number;
    cardCategory: CardCategory;
    langSupports: LangSupport[];
}
