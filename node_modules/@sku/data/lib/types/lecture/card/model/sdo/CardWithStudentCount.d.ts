import { PolyglotString } from '../../../../shared';
import { StudentCount } from '../../../student/model';
export interface CardWithStudentCount {
    cardId: string;
    collegeId: string;
    feedbackCommentId: string;
    name: PolyglotString;
    studentCount: StudentCount;
}
