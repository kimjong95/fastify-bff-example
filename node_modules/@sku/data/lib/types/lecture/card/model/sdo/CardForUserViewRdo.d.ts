import { DifficultyLevel, LangSupport, LearningState, PolyglotString, ProposalState } from '../../../../shared';
import { CardType } from '../vo/CardType';
import { LiveCard } from '../vo/LiveCard';
import { LearningType } from '../vo/LearningType';
import { EnrollmentCard } from '../EnrollmentCard';
export interface CardForUserViewRdo {
    id: string;
    langSupports: LangSupport[];
    name: PolyglotString;
    cardType: CardType;
    type: LearningType;
    thumbnailImagePath: string;
    simpleDescription: PolyglotString;
    difficultyLevel: DifficultyLevel;
    mainCollegeId: string;
    required: boolean;
    learningTime: number;
    additionalLearningTime: number;
    paid: boolean;
    stampCount: number;
    passedStudentCount: number;
    studentCount: number;
    starCount: number;
    badgeCount: number;
    modifiedTime: number;
    passedTime: number;
    registeredTime: number;
    round: number;
    learningState: LearningState;
    proposalState: ProposalState;
    liveCard: LiveCard | null;
    enrollmentCard: EnrollmentCard | null;
}
