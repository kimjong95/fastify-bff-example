import { EnrollmentCard } from './../EnrollmentCard';
import { PolyglotString, LearningState, ProposalState, LangSupport } from '../../../../shared';
import { CardType } from '../vo/CardType';
import { DifficultyLevel } from '../../../../shared';
export interface CardForUserViewRdo {
    id: string;
    langSupports: LangSupport[];
    name: PolyglotString;
    type: CardType;
    thumbnailImagePath: string;
    simpleDescription: PolyglotString;
    difficultyLevel: DifficultyLevel;
    mainCollegeId: string;
    required: boolean;
    learningTime: number;
    additionalLearningTime: number;
    paid: boolean;
    stampCount: number;
    passedStudentCount: number;
    studentCount: number;
    starCount: number;
    badgeCount: number;
    modifiedTime: number;
    passedTime: number;
    learningState: LearningState;
    proposalSate: ProposalState;
    enrollmentCard?: EnrollmentCard;
}
