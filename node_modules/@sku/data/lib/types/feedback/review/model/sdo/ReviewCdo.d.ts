import { DenizenKey, IdName } from '../../../../shared';
export interface ReviewCdo {
    writer: IdName;
    title: string;
    opinion: string;
    selectedStar: number;
    version: string;
    denizenKey: DenizenKey;
}
