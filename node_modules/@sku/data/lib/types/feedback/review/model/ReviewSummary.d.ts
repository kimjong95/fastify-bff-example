import { DramaEntity, IntPair } from '../../../shared';
export interface ReviewSummary extends DramaEntity {
    versionBased: boolean;
    maxStarCount: number;
    reviewerCount: number;
    average: number;
    starCounts: IntPair[];
    versionStarCountMap: Map<string, IntPair[]>;
    versionReviewerCountMap: Map<string, number>;
}
