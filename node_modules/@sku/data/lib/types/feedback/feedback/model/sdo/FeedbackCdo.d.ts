import { AudienceKey, IdName } from '../../../../shared';
import { CommentConfig } from '../../../comment/model';
export interface FeedbackCdo {
    title: string;
    audienceKey: AudienceKey;
    sourceEntity: IdName;
    commentConfig: CommentConfig;
}
