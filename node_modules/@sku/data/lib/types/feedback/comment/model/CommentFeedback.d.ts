import { DramaEntity, IdName } from '../../../shared';
import { CommentConfig } from './index';
export interface CommentFeedback extends DramaEntity {
    title: string;
    config: CommentConfig;
    time: number;
    sourceEntity: IdName;
}
