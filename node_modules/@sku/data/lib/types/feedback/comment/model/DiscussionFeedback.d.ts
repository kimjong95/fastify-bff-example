import { DramaEntity, IdName, RelatedUrl } from '../../../shared';
import { CommentConfig } from './CommentConfig';
export interface DiscussionFeedback extends DramaEntity {
    title: string;
    config: CommentConfig;
    time: number;
    sourceEntity: IdName;
    content: string;
    relatedUrlList: RelatedUrl[];
    depotId: string;
}
