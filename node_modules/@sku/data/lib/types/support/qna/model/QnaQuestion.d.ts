import { DomainEntity, PolyglotString } from '../../../shared';
import { RequestChannel } from './vo/RequestChannel';
import { QuestionState } from './vo/QuestionState';
export interface QnaQuestion extends DomainEntity {
    denizenId: string;
    requestChannel: RequestChannel;
    mainCategoryId: string;
    subCategoryId: string;
    relatedCardId: string;
    relatedCardName: PolyglotString;
    relatedQuestionId: string;
    operatorIds: string[];
    title: string;
    content: string;
    depotId: string;
    state: QuestionState;
    visibleForQuestioner: boolean;
    registeredTime: number;
    modifiedTime: number;
}
