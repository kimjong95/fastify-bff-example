import { SimpleUserIdentity } from '../../../../shared';
import { OperatorWithUserIdentityRom } from '../../../operator/model';
import { QnaEmailHistory } from '../QnaEmailHistory';
import { QnaQuestion } from '../QnaQuestion';
import { QnaAnswer } from '../QnaAnswer';
export interface QnaDetailRom {
    question: QnaQuestion;
    answer: QnaAnswer;
    operators: OperatorWithUserIdentityRom[];
    latestOperatorSentEmail: QnaEmailHistory;
    inquirerIdentity: SimpleUserIdentity;
}
