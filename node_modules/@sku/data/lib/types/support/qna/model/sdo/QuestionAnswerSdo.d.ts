import { QuestionState } from '../vo/QuestionState';
export interface QuestionAnswerSdo {
    questionId: string;
    content: string;
    depotId: string;
    memo: string;
    state: QuestionState;
    checkMail: boolean;
}
