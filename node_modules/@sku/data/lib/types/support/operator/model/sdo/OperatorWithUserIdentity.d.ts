import { SimpleUserIdentity } from '../../../../shared';
import { SupportOperator } from '../SupportOperator';
export interface OperatorWithUserIdentity {
    operator: SupportOperator;
    userIdentity: SimpleUserIdentity;
}
