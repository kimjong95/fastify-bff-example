import { DomainEntity } from '../../../shared';
export interface SupportOperator extends DomainEntity {
    operatorGroupId: string;
    denizenId: string;
    registrant: string;
    registeredTime: number;
    modifier: string;
    modifiedTime: number;
}
