import { DomainEntity, LangSupport, PatronKey, PolyglotString } from '../../../shared';
export interface BadgeCategory extends DomainEntity {
    patronKey: PatronKey;
    name: PolyglotString;
    langSupports: LangSupport[];
    themeColor: string;
    cineroomId: string;
    displayOrder: number;
    registrantName: PolyglotString;
    registeredTime: number;
}
