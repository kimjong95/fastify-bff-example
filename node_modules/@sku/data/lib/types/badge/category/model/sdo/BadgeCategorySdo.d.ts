import { PolyglotString, LangSupport } from '../../../../shared';
export interface BadgeCategorySdo {
    name: PolyglotString;
    langSupports: LangSupport[];
    iconPath: string;
    backgroundImagePath: string;
    topImagePath: string;
    themeColor: string;
}
