import { BadgeState } from '../vo/BadgeState';
import { BadgeType } from '../vo/BadgeType';
import { BadgeLevel } from '../vo/BadgeLevel';
export interface BadgeQdo {
    startDate: number;
    endDate: number;
    cineroomId: string;
    categoryId: string;
    type: BadgeType;
    level: BadgeLevel;
    issueAutomatically: boolean;
    additionalRequirementsNeeded: boolean;
    state: BadgeState;
    name: string;
    registrantName: string;
    offset: number;
    limit: number;
    groupSequences: number[];
}
